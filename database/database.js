//create table
// sql = `CREATE TABLE IF NOT EXISTS servers(ownerID, serverID, channels, membership, inactivityTime, channelsLastChanged)`;
// db.run(sql);

//insert data in db
// sql = `INSERT INTO users (userID, username, membership, servers) VALUES (?,?,?,?)`;
// db.run(sql,['975206289879756862','donuts_anonymous#0001', 'Basic', ['1038092563120390214']], (err) => {
//     if (err) return console.error(err.message);
// });

//query the database
